{
  "name": "azure-openai-service-info",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^3.17.3",
    "@azure/identity": "^3.2.4",
    "@azure/storage-blob": "^12.15.0",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^16.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
